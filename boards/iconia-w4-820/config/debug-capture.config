# Built-in FAT/NLS so an init-wrapper can mount the ESP (vfat) for dmesg capture
# without modules. Part of the working kernel #5. Harmless in production.
CONFIG_FAT_FS=y
CONFIG_VFAT_FS=y
CONFIG_NLS_CODEPAGE_437=y
CONFIG_NLS_ISO8859_1=y
